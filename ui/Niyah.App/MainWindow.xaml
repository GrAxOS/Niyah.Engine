<Window x:Class="Niyah.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Niyah.Engine"
        Width="1440" Height="900"
        MinWidth="1100" MinHeight="700"
        Background="{StaticResource WindowBrush}"
        Foreground="{StaticResource TextBrush}">
    <Grid Margin="18">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="280" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="56" />
            <RowDefinition Height="*" />
            <RowDefinition Height="46" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.ColumnSpan="3" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="14,8">
            <DockPanel>
                <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" FontSize="22" FontWeight="SemiBold" Text="Niyah.Engine" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <TextBlock x:Name="StatusText" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" Text="Ready" Margin="0,0,12,0" />
                    <TextBox x:Name="QueryBox" Width="420" Height="34" VerticalAlignment="Center" Background="#0D1116" BorderBrush="{StaticResource BorderBrush}" Foreground="{StaticResource TextBrush}" Padding="10,7" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" Margin="0,14,14,14" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock FontSize="16" FontWeight="SemiBold" Text="Knowledge Graph" />
                <TreeView Grid.Row="1" Margin="0,14,0,0" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextBrush}">
                    <TreeViewItem Header="Nodes" />
                    <TreeViewItem Header="Relations" />
                    <TreeViewItem Header="Evidence" />
                    <TreeViewItem Header="Sources" />
                </TreeView>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Grid.Column="1" Margin="0,14,14,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="170" />
            </Grid.RowDefinitions>
            <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="18">
                <TextBox x:Name="ConversationBox" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextBrush}" FontSize="15" />
            </Border>
            <Border Grid.Row="1" Margin="0,14,0,0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="38" />
                    </Grid.RowDefinitions>
                    <TextBox x:Name="ComposerBox" AcceptsReturn="True" TextWrapping="Wrap" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextBrush}" FontSize="15" />
                    <DockPanel Grid.Row="1" LastChildFill="False">
                        <Button DockPanel.Dock="Left" Width="110" Content="Attach files" Click="Attach_Click" />
                        <TextBlock VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" Text="Evidence classification remains explicit." Margin="10,0,0,0" />
                        <Button DockPanel.Dock="Right" Width="100" Content="Send" Click="Send_Click" Background="{StaticResource AccentBrush}" Foreground="White" BorderThickness="0" />
                    </DockPanel>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="1" Grid.Column="2" Margin="0,14,0,14" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock FontSize="16" FontWeight="SemiBold" Text="Evidence" />
                <StackPanel Grid.Row="1" Margin="0,14,0,0">
                    <TextBlock Text="FACT" Margin="0,0,0,8" />
                    <TextBlock Text="INFERENCE" Margin="0,0,0,8" />
                    <TextBlock Text="UNCERTAIN" Margin="0,0,0,8" />
                    <TextBlock Text="CONFLICTED" Margin="0,0,0,8" />
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="Sources" Margin="4" Padding="12,4" />
            <Button Content="Evidence" Margin="4" Padding="12,4" />
            <Button Content="Graph" Margin="4" Padding="12,4" />
            <Button Content="Code" Margin="4" Padding="12,4" />
            <Button Content="Network" Margin="4" Padding="12,4" />
            <Button Content="System" Margin="4" Padding="12,4" />
            <Button Content="History" Margin="4" Padding="12,4" />
        </StackPanel>
    </Grid>
</Window>
